{
  "name": "suggestion-box",
  "version": "1.0.0",
  "description": "- Use Postman and Studio 3T to test your routes\r - Use MongoDB and Mongoose and name the database `suggestion-box`\r - Use Async and Await (Don't forget the try and catch block)\r - Use controllers for business logic.\r - Remember to install your dependencies (mongoose)`\r - Only `.gitignore` the `node_modules`",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.2",
    "morgan": "^1.10.0"
  }
}
